
1.1.
pip install Django djangorestframework django-filter

1.2.
django-admin startproject config .

1.3.
Зарегистрировать drf в проекте
INSTALLED_APPS += [
    'rest_framework',
    'django_filters',
]

2. Создать файл в .env

3. Вынести константы в .env
SECRET_KEY="your-project-key"
DEBUG=True
ALLOWED_HOSTS="127.0.0.1 localhost"

4. Подключить .env в проект

Установка зависимостей
pip install django-environ

изменить файл settings.py

import environ

root = environ.Path(__file__)  - 2
env = environ.Env()
environ.Env.read_env(env.str(root(), '.env'))


запуск  python.exe .\manage.py runserver

pip freeze > requirements.txt
pip install -r .\requirements.txt

5. Заменить констанкты данными из .env
SECRET_KEY = env.str('SECRET_KEY')
DEBUG = env.bool('DEBUG', default=False)
ALLOWED_HOSTS = env.str('ALLOWED_HOSTS', default='').split('')

6. Определяем BASE_DIR
BASE_DIR = root()

7. Создать БД в PostgreSQL
ps

